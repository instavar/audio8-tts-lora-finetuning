name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  source-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check out pinned evaluation contract
        uses: actions/checkout@v5
        with:
          repository: instavar/instavar-voice-evaluation
          ref: 8c0fb66a592c73f801a289aabd242e03a6849115
          path: .instavar-voice-evaluation
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python -m compileall -q audio8_tts_data.py audio8_tts_infer.py audio8_tts_prepare.py audio8_tts_resume.py audio8_tts_sft.py scripts/create_initial_lora.py tests
      - run: python -m unittest discover -s tests -v
        env:
          PYTHONPATH: .instavar-voice-evaluation
      - run: bash -n audio8_tts_lora.sh audio8_tts_sft.sh
